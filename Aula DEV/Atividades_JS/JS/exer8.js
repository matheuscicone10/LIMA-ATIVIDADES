function verificar(){
    var senha = document.getElementById("senha").value;
    var minusculo = /[a-z]/;
    var maiusculo = /[A-Z]/;
    var num = /[0-9]/;

    if(senha == ""){
        alert("Favor preencha o campo!");
    }else if(senha = minusculo){

    }else if(senha = maiusculo){

    }else if (senha = num){

    }
}